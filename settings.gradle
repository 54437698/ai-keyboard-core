pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()            // <--- This is the "Key" to LiteRT
        mavenCentral()
    }
}

rootProject.name = "ai_keyboard"
include ':app'
