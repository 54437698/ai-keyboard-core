pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()            // This hits https://maven.google.com/
        mavenCentral()
    }
}
rootProject.name = "ai-keyboard-core"
include ':app'
