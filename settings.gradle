pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()            // <--- This MUST be here
        mavenCentral()      // <--- This MUST be here
    }
}
rootProject.name = "ai_keyboard"
include ':app'
