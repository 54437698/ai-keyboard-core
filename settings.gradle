pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    // CHANGE: Allow repositories to be defined in build.gradle
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT) 
    repositories {
        google()
        mavenCentral()
        // Adding the PyTorch link here too for double-safety
        maven { url 'https://ossci-android.s3.amazonaws.com/xml/nightly' }
    }
}
rootProject.name = "ai-keyboard-core"
include ':app'
