pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()            // This is where LiteRT 2.1.0 lives
        mavenCentral()
    }
}
rootProject.name = "ai_keyboard"
include ':app'
