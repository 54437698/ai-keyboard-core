pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()            // <--- Essential
        mavenCentral()
        // Add this specific mirror just in case the runner is being stubborn
        maven { url "https://maven.google.com" } 
    }
}
rootProject.name = "ai-keyboard-core"
include ':app'
